# 확인된 문제

## 컨베이어 벨트 연결 문제

- 상태: 해결
- 현상: 서로 이어서 설치한 컨베이어 벨트가 정상적으로 연결되거나 아이템을 전달하지 않는 경우가 있다.
- 기대 동작: 앞 벨트의 출력 방향이 다음 벨트 또는 건물의 입력 포트를 향하면 하나의 운송 경로로 연결되어야 한다.
- 확인할 항목:
  - 인접 벨트 사이의 출력 방향 판정
  - 벨트에서 다음 벨트로 아이템을 넘기는 조건
  - 벨트와 건물 입력 포트의 좌표 판정
  - 회전된 벨트의 화면 표시 방향과 실제 데이터 방향 일치 여부
- 해결 내용: 한 틱이 시작될 때 아이템이 있던 벨트만 한 칸씩 이동시키되, 앞 벨트가 비운 자리를 같은 틱 안에 뒤 벨트가 이어받도록 전달 순서를 반복 계산한다. 따라서 꽉 찬 직선·곡선 벨트도 중간이 끊긴 것처럼 멈추지 않는다.

## 청크 공간 확장 필요

- 상태: 적용 완료
- 현상: 현재 한 청크의 건설 공간이 좁아 생산 설비와 컨베이어 벨트를 배치하기 어렵다.
- 요구 사항: 기존 게임 규칙과 기능은 모두 그대로 유지하고, 한 청크의 크기만 `10×10`타일에서 `15×15`타일로 변경한다.
- 유지할 항목:
  - 전체 맵의 `31×31`청크 구성
  - 건물과 광맥의 크기
  - 광맥 수·티어·생성 규칙
  - 청크 해금 조건과 가격
  - 생산, 전력, 재고, 판매 및 저장 규칙
- 확인할 항목:
  - 청크 경계와 좌표 계산
  - 시작 청크와 코어 배치
  - 광맥 생성 위치와 잠긴 청크 탐사 범위
  - 기존 저장 데이터 호환 여부
- 적용 내용: 청크를 `15×15`타일로 확장하고 코어, 광맥, 탐사 표식, 카메라 기준 좌표를 새 크기에 맞췄다. 기존 `10×10` 저장 데이터는 건물과 벨트의 청크 내부 좌표를 유지한 채 새 좌표계로 자동 변환한다.

두 문제를 현행 코드에 반영했다.
